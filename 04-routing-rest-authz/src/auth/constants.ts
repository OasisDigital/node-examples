export const jwtConstants = {
  // IMPORTANT: For this class example, we use a trivial key included in the
  // source code. In production, instead this would be a secret managed like any
  // other authentication or security credential.
  secret: 'DontStoreSecretsInYourSourceCode',
};
